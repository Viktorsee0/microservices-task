server:
  port: 9091

spring:
  application:
    name: authentication-service

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
      #http://eureka-service:8761/eureka

logging:
  level:
    org.springframework.amqp.rabbit.connection.CachingConnectionFactory: WARN

keycloak:
  serverUrl: http://localhost:8282
  realm: master
  client:
    id: admin-cli
  username: admin
  password: admin
  token:
    client: specific-group-client
    endpoint: http://localhost:8282/realms/specific-group-realm/protocol/openid-connect/token
    secret: C2fMeRClc4Muy0mk4uDpss1OGZuOPyl9